TUNNEL_TOKEN=change-me

# Frontend API base URL — leave empty for same-origin nginx proxy (production default)
# Set to http://localhost:3000 when running the api separately in dev
VITE_API_BASE_URL=
